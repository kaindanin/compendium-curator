{
  "name": "compendium-curator",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node tools/check-project.mjs",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test",
    "release:build": "pwsh -NoProfile -File tools/build-release.ps1"
  },
  "engines": {
    "node": ">=22"
  }
}
